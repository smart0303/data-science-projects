version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: users
        columns:
          - name: user_id
            tests:
              - unique
              - not_null
          - name: email
            tests:
              - not_null
      - name: posts
        columns:
          - name: post_id
            tests:
              - unique
              - not_null
          - name: user_id
            tests:
              - not_null
